<%= render partial: 'shared/header_flat' %>

<%= render layout: 'layouts/content' do %>
  <% if alert = flash[:alert] %>
    <p class="py-2 px-3 bg-red-50 mb-5 text-red-500 font-medium rounded-lg inline-block" id="alert"><%= alert %></p>
  <% end %>

  <h1 class="text-4xl">Passwort vergessen?</h1>

  <%= form_with url: passwords_path, class: "contents" do |form| %>
    <div class="my-5">
      <%= form.email_field :email_address, required: true, autocomplete: "username", placeholder: "Gib deine E-Mail Adresse ein", value: params[:email_address], class: "block shadow rounded-md border border-gray-400 outline-none px-3 py-2 mt-2 w-full" %>
    </div>

    <div class="inline">
      <%= form.submit "Link zum Zurücksetzen senden" %>
    </div>
  <% end %>
<% end %>