<div>
  <h1 class="text-3xl font-semibold mb-3">
    Explore our products
  </h1>
  <p class="mb-6 text-gray-600 text-lg">
    Market is a simple e-commerce application built with Ruby on Rails and Tailwind CSS.
  </p>

  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6">
    <% @products.each do |product| %>
      <%= render partial: 'products/card', locals: { product: product } %>
    <% end %>
  </div>
</div>
