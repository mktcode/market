<div class="@container">
  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 2xl:grid-cols-6 gap-3 @5xl:gap-6">
    <% products.each do |product| %>
      <%= render partial: 'products/card', locals: { product: product } %>
    <% end %>
  </div>
</div>